# =============================================================
# Mihwar — Coolify DATABASE (Postgres) Environment Variables
# Paste into:  Database "mihwar" → Environment Variables
# =============================================================
# NOTE: Coolify already wires POSTGRES_USER, POSTGRES_PASSWORD,
# and POSTGRES_DB from the General → Username / Password /
# Initial Database fields. The variables below are OPTIONAL
# overrides + tuning. If you don't need to tune, leave the DB
# Environment Variables tab empty — the General fields are
# enough for the app to connect.
# =============================================================

# --- Postgres core (already set by Coolify "General" tab) ----
# Uncomment ONLY if you want to override the values from the UI.
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=bkFF56iX08N5JyS3YPxcKZf0riK6pspTtZPds5QvqtwZRCJw2tvfWQtqVy7epR7O
# POSTGRES_DB=postgres

# --- Locale & timezone (recommended) -------------------------
TZ=Asia/Riyadh
PGTZ=Asia/Riyadh
LANG=en_US.UTF-8
LC_ALL=en_US.UTF-8

# --- Init args (alternative to the General → "Initial Database
# Arguments" field). Leave empty unless you need them.
# POSTGRES_INITDB_ARGS=--encoding=UTF-8 --locale=en_US.UTF-8

# =============================================================
# Connection details — for reference, paste into the WEB APP's
# DATABASE_URL (already done in coolify.app.env.txt).
# Do NOT paste these into the DB env vars panel.
#
#   user     : postgres
#   password : bkFF56iX08N5JyS3YPxcKZf0riK6pspTtZPds5QvqtwZRCJw2tvfWQtqVy7epR7O
#   host     : mihwar          ← internal Docker hostname (the
#                                 resource name; verify by clicking
#                                 the eye icon on "Postgres URL
#                                 (internal)" in Coolify)
#   port     : 5432            ← internal port; always 5432
#   database : postgres        ← matches "Initial Database"
#
# Internal URL (use in app):
#   postgresql://postgres:<password>@mihwar:5432/postgres?schema=public
#
# Public URL (only if "Make it publicly available" is ON):
#   postgresql://postgres:<password>@<your-server-ip>:5432/postgres
#   (NOT recommended — keep the DB internal-only)
# =============================================================

# Security note:
# The "Make it publicly available" toggle is OFF by default and
# should stay OFF. Your app reaches the DB over the internal
# Docker network — public exposure is unnecessary and risky.
