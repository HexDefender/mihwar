# ─────────────────────────────────────────────────────────────
# Mihwar (محور) — Coolify Application Environment Variables
# Paste these into: Coolify → Application → Environment Variables
# Replace placeholder values where indicated.
# ─────────────────────────────────────────────────────────────

# ── DATABASE (REQUIRED) ──────────────────────────────────────
# After creating the PostgreSQL resource in Coolify, click it
# and copy the "Internal" connection string into this var.
# Format: postgresql://USER:PASSWORD@<service-host>:5432/DBNAME
DATABASE_URL=postgresql://postgres:CHANGE_ME@mihwar-postgres:5432/mihwar?schema=public

# ── AUTH (REQUIRED) ──────────────────────────────────────────
# AUTH_SECRET — already generated for you. Keep secret.
# To rotate: run `openssl rand -base64 48`
AUTH_SECRET=+sTdq56mfdJ+W6NE01UqShtAKcNF++Tcys8BIkP7VpuMKC1XNIiOzdmu0Jtoli2A

# AUTH_URL — must EXACTLY match the public URL Coolify binds.
# No trailing slash. Update after you bind your domain.
AUTH_URL=https://mihwar.your-domain.com

# Public app URL (used by client-side code)
NEXT_PUBLIC_APP_URL=https://mihwar.your-domain.com

# ── INITIAL ADMIN (used by seed on first boot) ───────────────
ADMIN_EMAIL=admin@mihwar.local
ADMIN_USERNAME=admin
ADMIN_PASSWORD=Mihwar@2026
ADMIN_NAME=مدير الفريق

# ── SEED CONTROL ─────────────────────────────────────────────
# Set to "true" on the FIRST deploy ONLY.
# After the database is populated, change this to "false"
# (or remove the variable entirely) so re-deploys don't reseed.
RUN_SEED=true

# ── RUNTIME ──────────────────────────────────────────────────
NODE_ENV=production
PORT=3000
HOSTNAME=0.0.0.0
NEXT_TELEMETRY_DISABLED=1

# ─────────────────────────────────────────────────────────────
# Coolify Application settings (NOT env vars — set in the UI):
#   • Build Pack:        Dockerfile
#   • Port:              3000
#   • Health Check Path: /api/health
#   • Repository:        https://github.com/HexDefender/mihwar
#   • Branch:            main
# ─────────────────────────────────────────────────────────────
